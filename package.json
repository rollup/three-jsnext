{
  "name": "three-jsnext",
  "description": "three.js, but futuristic",
  "jsnext:main": "./src/index.js",
  "repository": "https://github.com/rollup/three-jsnext",
  "scripts": {
    "prepublish": "npm test",
    "build": "node generate && rollup -i src/index.js -o dist/three.js -f umd -n THREE && uglifyjs -c -m -o dist/three.min.js -- dist/three.js",
    "test": "echo 'tests coming soon...'",
    "pretest": "npm run build",
    "update-three": "rm -rf three.js && git clone https://github.com/mrdoob/three.js && npm run update-pkg",
    "update-pkg": "node scripts/update-package.js"
  },
  "files": [
    "src/",
    "index.js",
    "README.md"
  ],
  "devDependencies": {
    "acorn": "^2.0.4",
    "babel": "^5.6.14",
    "magic-string": "^0.6.3",
    "sander": "^0.3.4"
  }
}
